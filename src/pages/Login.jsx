import React,{useState} from "react";
import {useLocation,useNavigate} from "react-router-dom";
import "./Login.css";
function Login(){const navigate=useNavigate(),location=useLocation(),[email,setEmail]=useState(location.state?.email||localStorage.getItem("borrowBoxEmail")||""),[password,setPassword]=useState(""),[showPassword,setShowPassword]=useState(false),[message,setMessage]=useState(location.state?.signupSuccess||"");
const handleLogin=e=>{e.preventDefault();const clean=email.trim().toLowerCase();if(!clean||!password)return setMessage("Please enter your email and password.");let users=[];try{users=JSON.parse(localStorage.getItem("borrowBoxUsers")||"[]")}catch{}const user=users.find(u=>String(u.email).toLowerCase()===clean&&u.password===password);if(!user)return setMessage("Invalid email or password. Please sign up first.");const session={...user};delete session.password;localStorage.setItem("borrowBoxUser",JSON.stringify(session));localStorage.setItem("borrowBoxEmail",clean);navigate("/home",{replace:true})};
return <div className="auth-page"><section className="auth-brand"><div className="brand-content"><div className="brand-logo">◇</div><h1>Borrow what you<br/><span>need.</span></h1><p>Borrow Box makes it simple for everyone to discover, borrow, rent and sell useful items in their community.</p><div className="feature-list"><div className="feature"><div className="feature-icon">🔍</div><div><strong>Find useful items</strong><small>Browse the built-in dataset instantly</small></div></div><div className="feature"><div className="feature-icon">🔄</div><div><strong>Borrow or rent</strong><small>Choose an option that fits your needs</small></div></div><div className="feature"><div className="feature-icon">💰</div><div><strong>Sell your items</strong><small>Manage your sharing activity</small></div></div></div></div><div className="brand-footer">Borrow Box • Frontend demo mode.</div></section><section className="auth-form-section"><div className="mobile-logo"><div className="brand-logo">◇</div><span>Borrow Box</span></div><div className="login-container"><div className="login-heading"><span className="welcome-tag">Welcome back</span><h2>Sign in to Borrow Box</h2><p>Login works locally — no server or OTP required.</p></div><form onSubmit={handleLogin}><div className="field"><label htmlFor="email">Email address</label><div className="input-wrapper"><span className="input-icon">@</span><input id="email" type="email" placeholder="you@example.com" value={email} onChange={e=>{setEmail(e.target.value);setMessage("")}} autoComplete="email"/></div></div><div className="field"><div className="password-label"><label htmlFor="password">Password</label></div><div className="input-wrapper"><span className="input-icon">•</span><input id="password" type={showPassword?"text":"password"} placeholder="Enter your password" value={password} onChange={e=>{setPassword(e.target.value);setMessage("")}} autoComplete="current-password"/><button type="button" className="show-password" onClick={()=>setShowPassword(v=>!v)}>{showPassword?"Hide":"Show"}</button></div></div>{message&&<div className="login-message">{message}</div>}<button type="submit" className="signin-button">Sign In <span>→</span></button></form><div className="signup-text">Don't have an account?<button type="button" onClick={()=>navigate("/signup")}>Sign up</button></div><div className="security-note">Your account stays in this browser using localStorage.</div></div></section></div>}
export default Login;